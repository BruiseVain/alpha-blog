<h1>Showing the article details</h1>

<p><strong>Title: </strong><%= @article.title %></p>
<p><strong>Description: </strong><%= @article.description %></p>

<table>
<tr>
  <td><%= link_to 'Edit', edit_article_path(@article) %></td>
  <td><%= button_to 'Delete', article_path(@article), :method => :delete, data: { confirm: "are u sure?"}%></td>
  <td><%= link_to 'Return to the articles page', articles_path %></td>
</tr>
</table>